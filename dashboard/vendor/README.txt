Three.js is loaded at runtime from https://esm.sh/three@0.160.0 via an import
map in index.html. No vendored bundle is required because the dashboard runs
directly from static hosting without a build step.
